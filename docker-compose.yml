version: "3.8"

services:
  # Бэкенд сервис
  backend:
    build:
      context: ./back
      dockerfile: Dockerfile
    container_name: nareshka-backend
    expose:
      - "4000"
    environment:
      - PORT=4000
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - SECRET_KEY=${SECRET_KEY}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-http://localhost:3000}
    networks:
      - nareshka-network
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import requests; requests.get('http://127.0.0.1:4000/health')",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Фронтенд сервис
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    container_name: nareshka-frontend
    expose:
      - "80"
    depends_on:
      - backend
    networks:
      - nareshka-network
    restart: unless-stopped
    labels:
      - traefik.http.services.frontend.loadbalancer.server.port=80
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1"]
      interval: 30s
      timeout: 10s
      retries: 3

# Общая сеть для контейнеров
networks:
  nareshka-network:
    driver: bridge
    name: nareshka-network
